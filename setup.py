from setuptools import setup

setup(name='cli_colors',
      version='0.1',
      description='A light library for coloring terminal output',
      url='http://github.com/omarnaman/cli_colors',
      author='omarnaman',
      license='MIT',
      packages=['cli_colors'],
      zip_safe=False)
