from setuptools import setup

setup(name='cli_colors',
      version='0.1',
      description='A light library for coloring terminal output',
      url='http://github.com/sudo-Smiley/cli_colors',
      author='sudo-Smiley',
      license='MIT',
      packages=['cli_colors'],
      zip_safe=False)